@model IEnumerable<CinemaArchiveProje.Models.Actor>

@{
    ViewData["Title"] = "Aktörler";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Name)</th>
            <th>@Html.DisplayNameFor(model => model.Biography)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var actor in Model)
        {
            <tr>
                <td>@actor.Name</td>
                <td>@actor.Biography</td>
                <td>
                    <a href="@Url.Action("Edit", new { id = actor.Id })">Edit</a> |
                    <a href="@Url.Action("Delete", new { id = actor.Id })">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<p>
    <a href="@Url.Action("Create")" class="btn btn-primary">Yeni Aktör Ekle</a>
</p>
